- name: init
  command: '{{ k8s_kubeadm }} init --config /etc/kubernetes/kubeadm-config.yaml'
  args:
    creates: /etc/kubernetes/manifests/kube-apiserver.yaml
  tags:
    - k8s
    - k8s-leader
    - k8s-leader-init
