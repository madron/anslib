- name: reset
  command: '{{ k8s_kubeadm }} reset -f'


- name: remove /etc/kubernetes directory
  file:
    path: /etc/kubernetes
    state: absent


# - name: remove /etc/systemd/system/iptables.rules
#   file:
#     path: /etc/systemd/system/iptables.rules
#     state: absent


- name: reset_iptables command
  template:
    src: reset_iptables
    dest: /usr/local/bin/reset_iptables
    mode: 0755
    owner: root
    group: root


- name: reset iptables
  command: /usr/local/bin/reset_iptables
