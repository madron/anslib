- name: required packages
  apt: >
    name={{ item }}
    state=present
  with_items:
    - mysql-server
    - python-mysqldb

- name: mysql config
  template: >
    src=my.cnf
    dest=/etc/mysql/my.cnf
    mode=0644
    owner=root
    group=root
  notify: restart mysql
