sentry_version: 8.15

# docker run --rm sentry config generate-secret-key
# docker run -it --rm -e SENTRY_SECRET_KEY='<secret-key>' --link sentry_postgres:postgres --link sentry_redis:redis sentry upgrade
secret_key: ''

sentry_port: 80

docker_image_download: false

# sentry.conf
sentry_allowed_hosts: '*'
sentry_url_prefix: 'http://localhost'
sentry_email_host: 'localhost'
#sentry_email_password: ''
#sentry_email_user: ''
#sentry_email_port: 25
#sentry_email_use_tls: false

# The email address to send on behalf of
sentry_server_email: 'root@localhost'
