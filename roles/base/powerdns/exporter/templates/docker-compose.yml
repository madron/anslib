version: '2'

services:
  server:
    container_name: {{ project }}
    image: janeczku/powerdns-exporter:{{ version }}
    restart: always
    network_mode: host
    command: -listen-address=":{{ port }}" -api-url="{{ api_url }}" -api-key="{{ api_key }}"
