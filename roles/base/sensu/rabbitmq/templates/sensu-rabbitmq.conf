# {{ ansible_managed }}
{% set CONTAINER_NAME = sensu_prefix + '-rabbitmq' %}
{% set AMBASSADOR_NAME = CONTAINER_NAME + '-ambassador' %}

description "{{ CONTAINER_NAME }} container"
author "Massimiliano Ravelli <m.ravelli@mastervoice.it>"

pre-start script
    # Ambassador
    if ! docker inspect {{ AMBASSADOR_NAME }} > /dev/null 2>&1; then
        /usr/bin/docker run \
            -d \
            --restart="always" \
            --name {{ AMBASSADOR_NAME }} \
            --hostname {{ AMBASSADOR_NAME }} \
            --volume /var/run/docker.sock:/var/run/docker.sock \
            cpuguy83/docker-grand-ambassador --name {{ CONTAINER_NAME }}
    fi
    # Container
    if ! docker inspect {{ CONTAINER_NAME }} > /dev/null 2>&1; then
        /usr/bin/docker run \
            -d \
            --restart="always" \
            --name {{ CONTAINER_NAME }} \
            --hostname {{ CONTAINER_NAME }} \
            --volume {{ sensu_rabbitmq_ssl_base_dir }}/{{ sensu_prefix }}:/etc/rabbitmq/ssl:ro \
            --publish {{ sensu_rabbitmq_port }}:5671 \
            --publish {{ sensu_rabbitmq_web_port }}:15672 \
            --env=RABBITMQ_PASSWORD={{ sensu_rabbitmq_password }} \
            mtek/sensu_rabbitmq
    fi
end script

post-stop script
    /usr/bin/docker rm -fv {{ CONTAINER_NAME }}
end script
