registry_prefix: registry
registry_tag: 0.9.1

registry_options: []

# Default: port 5000 is exposed but not published
# registry_port: 5000

# Dafault: directory location is up to data container
# registry_data_dir: /var/lib/registry
