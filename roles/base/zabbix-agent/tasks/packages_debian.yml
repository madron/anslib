- name: repository
  apt_repository: >
    repo=ppa:nginx/stable
    state=present
  tags: nginx


- name: required packages
  apt:
    name: '{{ item }}'
    state: latest
  with_items: zabbix_agent_packages
  tags:
    - zabbix-agent
