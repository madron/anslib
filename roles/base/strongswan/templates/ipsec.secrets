: PSK "{{ server_key }}"
: RSA vpnHostKey.der

{% for host in vpn_clients_v1 %}
    {{ host.clientname }} : XAUTH "{{ host.password }}",
{% endfor %}
{% for host in vpn_clients_v2 %}
    {{ host.clientname }} : EAP "{{ host.password }}",
{% endfor %}

