- name: create /etc/kubernetes/manifests directory
  file:
    path: /etc/kubernetes/manifests
    state: directory
    owner: root
    group: root
    mode: 0755
  tags:
    - k8s-kube-proxy


- name: config /etc/kubernetes/manifests/kube-proxy.yaml
  template:
    src: kube-proxy.yaml
    dest: /etc/kubernetes/manifests/kube-proxy.yaml
    mode: 0644
    owner: root
    group: root
  register: config
  tags:
    - k8s-kube-proxy
